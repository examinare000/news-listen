# ============================================================
# news-listen — 環境変数テンプレート
# cp .env.example .env して実際の値を入力する
# .env は絶対にコミットしないこと
# ============================================================

# ── GCP プロジェクト ──────────────────────────────────────────
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=asia-northeast1

# Cloud Storage バケット名（{GCP_PROJECT_ID}-podcasts を推奨）
GCP_BUCKET_NAME=your-gcp-project-id-podcasts

# Artifact Registry リポジトリ名
GCP_AR_REPO=news-listen

# ── AI APIs ──────────────────────────────────────────────────
# Google AI Studio: https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...

# ── アプリ認証 ────────────────────────────────────────────────
# Web/iOS → バックエンド API の認証キー
# 生成方法: openssl rand -hex 32
API_KEY=your-random-api-key-here

# ── バッチジョブ ──────────────────────────────────────────────
# 処理対象のユーザー ID（rss-fetcher・recommendation・podcast-generator で必須）
USER_ID=your-user-id

# Podcast スクリプトの難易度（podcast-generator のみ。省略時: toeic_900）
# 有効値: toeic_600 / toeic_900 / ielts_55 / ielts_70 / eiken_2 / eiken_pre1
DIFFICULTY=toeic_900

# Dockerfile.jobs で実行するジョブモジュールを指定
# 例: jobs.rss_fetcher.main / jobs.recommendation.main / jobs.podcast_generator.main
JOB_MODULE=jobs.rss_fetcher.main

# ── ローカル開発 ──────────────────────────────────────────────
# バックエンド API の URL（ローカル開発時）
API_BASE_URL=http://localhost:8080

# Firestore エミュレーター（ローカルテスト時のみ設定）
# FIRESTORE_EMULATOR_HOST=localhost:8088

# Cloud Storage エミュレーター（ローカルテスト時のみ設定）
# STORAGE_EMULATOR_HOST=localhost:9023

# ── Secret Manager 参照名（本番用・変更不要） ─────────────────
SECRET_NAME_API_KEY=news-listen-api-key
SECRET_NAME_GEMINI=news-listen-gemini-key
